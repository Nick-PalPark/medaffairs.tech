# DEPRECATED: This workflow has been replaced by sync_from_medaffairs_articles.yml
# which includes data transformation capabilities.
# This file is kept for reference but should not be used.

name: [DEPRECATED] Sync articles from medaffairs-articles

on:
  # Disabled - use sync_from_medaffairs_articles.yml instead
  workflow_dispatch:
    inputs:
      deprecated_notice:
        description: 'This workflow is deprecated. Use sync_from_medaffairs_articles.yml instead.'
        required: false
        default: 'acknowledged'

permissions:
  contents: write

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "⚠️  This workflow is DEPRECATED"
          echo "Please use sync_from_medaffairs_articles.yml instead"
          echo "The new workflow includes data transformation capabilities"
          exit 1